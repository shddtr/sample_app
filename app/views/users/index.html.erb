<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <!-- @usersのそれぞれに対して_user.html.erbを使う -->
  <%= render @users %>
  <!-- <% @users.each do |user| %> -->
  <!-- <\!-- _user.html.erbを探しにいく -\-> -->
  <!-- <%= render user %> -->
  <!-- <% end %> -->
</ul>

<%= will_paginate %>
